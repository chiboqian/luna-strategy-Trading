create a cli script that would buy x amount of dollars of a single stock. only buy when there is no such stock in the account and the account that the cash to purchase. create a limit order, by default using the ask price but with an option of having an offset (for example -price_offset 0.01 which will be $0.01 more than the asking proce) with an option of using market order instead of a limit order. create the order with stop loss option with a X%  loss which need be calculated from the stock proce. default the stop loss percent  to 5% and make it customised from command line 
